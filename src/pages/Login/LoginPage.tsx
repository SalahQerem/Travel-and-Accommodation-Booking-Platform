import { Container } from "@mui/material";

const LoginPage = () => {
  return (
    <Container sx={{ py: { xs: 4, sm: 8 }, height: 300, mt: 20 }}>
      Login page
    </Container>
  );
};

export default LoginPage;
