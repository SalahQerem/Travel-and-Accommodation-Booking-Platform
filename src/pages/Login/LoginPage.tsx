import { Container } from "@mui/material";

const LoginPage = () => {
  return (
    <Container sx={{ py: { xs: 4, sm: 8 }, height: "100vh" }}>
      Login page
    </Container>
  );
};

export default LoginPage;
